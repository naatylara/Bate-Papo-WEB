<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CADASTRO DE USUÁRIO</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <style>
          body {
            background-color: #333;
            color: white;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
            margin: 0;
            font-family: 'Arial', sans-serif;
        }

        .container {
            display: flex;
            flex-direction: column;
            width: 800px;
            padding: 20px;
            background-color: #333;
            border-radius: 15px;
            box-shadow: 0 0 20px rgba(255, 255, 255, 0.1);
            margin-top: 40px;
        }

        h1 {
            text-align: center;
            font-size: 24px;
            font-weight: bold;
            color: #17a2b8;
            margin-bottom: 20px;
        }

        form {
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        label {
            margin-bottom: 10px;
        }

        select, input {
            width: 100%;
            padding: 10px;
            margin-bottom: 20px;
            background-color: #444;
            color: white;
            border: 1px solid #666;
        }

        button {
            width: 100%;
            padding: 10px;
            background-color: transparent;
            border: 1px solid #17a2b8;
            color: #17a2b8;
            cursor: pointer;
        }

        button:hover {
            background-color: #17a2b8;
            border-color: #17a2b8;
            color: white;
        }

        .messages-container {
            display: flex;
            flex-direction: column;
            margin-top: 20px;
        }

        .message {
            background-color: #444;
            padding: 10px;
            margin-bottom: 10px;
            border-radius: 8px;
        }

        .message strong {
            color: #17a2b8;
        }

        footer {
            text-align: center;
            margin-top: auto;
            padding: 10px;
            background-color: #333;
            color: white;
            width: 100%;
        }
    </style>
</head>
<body style="display: flex; flex-direction: column; min-height: 100vh;">
    <div class="container">
        <h1>Bate-Papo</h1>
        <form action="/enviarMensagem" method="POST">
            <label for="usuarios">Usuário:</label>
            <select id="usuarios" name="usuario">
                <% for (const usuario of listaUsuarios) { %>
                    <option value="<%= usuario.nome %>"><%= usuario.nome %></option>
                <% } %>
            </select>
            <label for="mensagem">Mensagem:</label>
            <input type="text" id="mensagem" name="mensagem" required>
            <button type="submit">Enviar</button>
        </form>

        <div class="messages-container">
            <!-- Exibir mensagens anteriores -->
            <% for (const mensagem of listaMensagens) { %>
                <div class="message"><strong><%= mensagem.usuario %>:</strong> <%= mensagem.texto %> - Enviada em <%= mensagem.data %></div>
            <% } %>
        </div>
    </div>
    <footer>
        <p>&copy; 2023 Nátaly Lara Moraes da Silva | PPI | UNOESTE | BATE-PAPO</p>
    </footer>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-MrcW6ZMFYlzcLA8Nl+NtUVF0sA7MsXsP1UyJoMp4YLEuNSfAP+JcXn/tWtIaxVXM" crossorigin="anonymous"></script>
</body>
</html>
